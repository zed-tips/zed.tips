# GitHub App 创建 - 详细步骤指南（含截图位置说明）

## 📍 第 1 步：找到 GitHub App 设置页面

### 位置：Organization Settings

1. 进入你的 GitHub
2. 点击右上角你的头像 → 选择 **"Settings"**（注意：是你的个人设置，不是 organization）

等等，我们需要的是 **organization level** 的 App，所以：

3. 改为：进入 **zed-tips organization**
   - 网址：https://github.com/zed-tips
4. 点击 organization 名称下方的 **"Settings"** 标签
5. 在左侧菜单找到 **"Developer settings"**
6. 点击 **"Developer settings"** → **"GitHub Apps"**

完整网址：https://github.com/organizations/zed-tips/settings/apps

---

## 📍 第 2 步：创建新 GitHub App

进入上面的网址后，你应该看到：

```
GitHub Apps
├─ Create a GitHub App （按钮）
└─ （现有的 Apps 列表）
```

**点击 "New GitHub App" 或 "Create a GitHub App" 按钮**

---

## 📍 第 3 步：填写 App 信息

你会看到一个表单，需要填写以下内容：

### 基本信息

| 字段 | 填写值 | 说明 |
|------|--------|------|
| **GitHub App name** | `zed-tips-ci` | App 的名称 |
| **Homepage URL** | `https://github.com/zed-tips` | 你的 organization 主页 |
| **Webhook** 章节 | 取消勾选 "Active" | 我们不需要 webhook |

### 权限配置

向下滚动，找到 **"Permissions"** 部分。

#### Repository permissions

这部分会有很多权限选项，你需要找到这两个：

```
Permissions
├─ Contents
│  └─ 选择：Read and write ✅
│
├─ Actions  
│  └─ 选择：Read and write ✅
│
├─ Administration
│  └─ 保持不选 ❌
│
├─ Checks
│  └─ 保持不选 ❌
│
└─ ... (其他权限都不选)
```

**关键步骤**：
1. 找到 **Contents** 行
2. 右侧的下拉菜单，选择 **"Read and write"**
3. 找到 **Actions** 行  
4. 右侧的下拉菜单，选择 **"Read and write"**

### Account permissions

这部分通常是这样设置：

```
Account permissions
├─ ... (都保持默认或不选)
```

不需要修改。

### Where can this GitHub App be installed?

找到这一部分，选择：

```
☑ Only on this account (zed-tips)
```

---

## 📍 第 4 步：创建 App

滚动到页面底部，点击：

```
[Create GitHub App] 按钮
```

如果一切正常，你应该会被重定向到 App 的详情页面。

---

## 🔑 第 5 步：生成私钥（最重要！）

这是最容易找错的地方。让我详细说明：

### 在 App 详情页找到私钥部分

创建完 App 后，你会在一个新页面上。这个页面有很多信息，你需要找到：

**左侧菜单**或**页面内容**中有这些部分：

```
App settings 页面
├─ General
├─ Permissions & events
├─ Install App
├─ ... 其他
└─ Private keys
   ├─ You haven't generated any private keys yet.
   ├─ [Generate a private key] 按钮
   └─ (生成后会显示列表)
```

### 具体位置：

**如果你看不到 "Private keys"，可能在以下位置**：

#### 位置 1：页面底部
- 向下滚动整个页面
- 最底部会有 "Private keys" 部分
- 看起来像这样：
  ```
  Private keys
  
  You haven't generated any private keys yet.
  
  Generate a private key
  ```

#### 位置 2：左侧菜单
- 看看左侧是否有菜单栏
- 找 "Private keys" 菜单项
- 点击进入

#### 位置 3：可能在 "General" 标签下
- 如果页面有多个标签
- 确保你在 "General" 或 "About" 标签
- "Private keys" 通常在 General 标签下

### 点击生成私钥

找到后，点击蓝色的 **"Generate a private key"** 按钮。

---

## 💾 第 6 步：下载和保存私钥

点击后，你会看到：

```
✅ New private key generated!

Your new private key has been generated and is displayed below.

-----BEGIN RSA PRIVATE KEY-----
MIIEpAIBAAKCAQEA... (很长的内容)
-----END RSA PRIVATE KEY-----

[Download] 按钮
```

### 重要！

1. **点击 "Download" 按钮**下载 `.pem` 文件
   - 或者手动复制整段文本（包括 BEGIN 和 END 行）

2. **保存这个文件到安全的位置**
   - 不要上传到 GitHub
   - 不要分享给任何人
   - 这相当于 GitHub 账户的密码

3. **记住位置**
   - 稍后配置 Secrets 时需要用到

---

## 🆔 第 7 步：获取 App ID

还在同一个 App 详情页，找到 **"About"** 或 **"General"** 部分的顶部。

你会看到：

```
GitHub App name: zed-tips-ci

App ID: 123456789    ← 这就是你需要的 App ID!

Client ID: Iv1.abc...
Client secret: abc...
```

### 记录 App ID

```
✏️ App ID: ________________

例如：App ID: 426754 (这只是示例)
```

---

## 📋 检查清单

到这里为止，你应该有：

- [ ] ✅ 创建了名为 "zed-tips-ci" 的 GitHub App
- [ ] ✅ 配置了权限：Contents (Read & write) 和 Actions (Read & write)
- [ ] ✅ 下载了私钥文件（.pem 文件）
- [ ] ✅ 记录了 App ID（数字，如 426754）

---

## 🔧 如果找不到 "Private keys"？

### 可能的原因和解决方案

#### 1️⃣ 你在错误的页面

- ❌ 错误：进入了 "Settings" → "Developer settings" → "GitHub Apps" 列表
- ✅ 正确：进入了某个具体 App 的详情页（应该能看到 App 名字）

**解决**：
- 找到你创建的 "zed-tips-ci" App
- 点击它进入详情页

#### 2️⃣ 页面没有完全加载

- 等待页面完全加载
- 刷新页面 F5

#### 3️⃣ 在错误的组织

- 确保你在 **zed-tips** organization
- 不是在个人账户下

#### 4️⃣ 浏览器问题

- 清除缓存
- 尝试另一个浏览器
- 使用无痕模式

---

## 🎯 完整路径总结

```
1. 进入 https://github.com/zed-tips
2. 点击 Settings
3. 点击 Developer settings (左侧菜单)
4. 点击 GitHub Apps
5. 看到已创建的 "zed-tips-ci" 
6. 点击 "zed-tips-ci" 进入详情页
7. 向下滚动找 "Private keys"
8. 点击 "Generate a private key"
9. 下载 .pem 文件或复制内容
10. 在同一页面顶部找到 App ID
11. 记录 App ID 和私钥
```

---

## 🚀 下一步

获得了 App ID 和私钥后，继续：

1. **在两个仓库中安装 App**
   - zed-tips-data
   - zed.tips

2. **配置 Secrets**（在 zed-tips-data 中）
   - GH_APP_ID
   - GH_APP_PRIVATE_KEY

详见 `QUICK_START.md` 中的安装步骤。

---

## 📞 还是找不到？

如果还是找不到，可能是 GitHub 界面更新了。在这种情况下：

1. **官方文档**：https://docs.github.com/en/developers/apps/building-github-apps/creating-a-github-app

2. **寻找帮助**：
   - 看 GitHub 官方文档中的截图
   - 在 GitHub Community Discussions 提问
   - 检查你的 GitHub 账户权限是否足够

3. **代替方案**（如果真的找不到）：
   - 虽然不如 GitHub App 好，但可以考虑使用 Personal Access Token（权限有限）
   - 但这样就需要调整我们的工作流程

---

**祝配置顺利！** 🎉

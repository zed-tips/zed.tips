# CI/CD 实现总结

## 📝 项目概述

这是一个完整的 CI/CD 解决方案，用于：
1. **PR 时验证** Tips 的格式和字段
2. **Merge 后自动触发** zed.tips 项目的重新构建
3. **自动部署** 最新的社区 Tips 到 https://zed.tips

---

## 🎯 已完成的工作

### ✅ 1. 创建验证脚本

**文件**：`scripts/validate-tips.js`

**功能**：
- ✅ 自动检测 PR 中修改的 `.md`/`.mdx` 文件
- ✅ 验证文件名格式（必须是 `kebab-case`: `my-tip-name.mdx`）
- ✅ 解析并验证 YAML Front Matter
- ✅ 检查所有必须字段存在且类型正确
- ✅ 验证字段值（枚举值、URL 格式、日期格式等）
- ✅ 限制 tags 数组最多 10 个元素

**验证规则**：

| 字段 | 类型 | 必须 | 约束 |
|------|------|------|------|
| `title` | string | ✅ | 非空 |
| `subtitle` | string | ✅ | 非空 |
| `category` | string | ✅ | 无限制（灵活性） |
| `difficulty` | string | ✅ | 无限制（灵活性） |
| `tags` | array | ✅ | 1-10 个元素 |
| `mediaType` | enum | ✅ | `"image"` 或 `"video"` |
| `mediaUrl` | string | ✅ | 有效 URL 格式 |
| `publishedAt` | date | ✅ | `YYYY-MM-DD` 格式 |
| `updatedAt` | date | ❌ | `YYYY-MM-DD` 格式（可选） |
| `featured` | boolean | ❌ | 可选 |
| `newInMonth` | date | ❌ | `YYYY-MM-DD` 格式（可选） |

---

### ✅ 2. 创建 PR 验证 Workflow

**文件**：`.github/workflows/validate-tips.yml`

**触发条件**：
- PR 提交到 `main` 分支
- PR 修改了 `tips/**/*.{md,mdx}` 文件

**工作流程**：
```
PR 创建/更新
    ↓
GitHub Actions 触发 validate-tips.yml
    ├─ Checkout 代码
    ├─ Setup Node.js 和 pnpm
    ├─ 安装依赖（gray-matter, zod）
    └─ 运行验证脚本
         ├─ 检查文件名格式
         ├─ 解析 Front Matter
         ├─ 验证 Schema
         └─ 报告错误/成功
    ↓
❌ 失败 → PR 显示红色 ❌，无法 merge
✅ 成功 → PR 显示绿色 ✅，可以 merge
```

**耗时**：约 30-40 秒

---

### ✅ 3. 创建自动触发 Workflow

**文件**：`.github/workflows/trigger-rebuild.yml`

**触发条件**：
- PR merge 到 `main` 分支
- 修改了 `tips/**/*.{md,mdx}` 文件

**工作流程**：
```
PR 合并到 main
    ↓
GitHub Actions 触发 trigger-rebuild.yml
    ├─ 使用 GitHub App 生成临时 token
    └─ 向 zed.tips 仓库发送 repository_dispatch 事件
    ↓
zed.tips 的 rebuild-from-tips-data.yml 被触发
```

**耗时**：约 5-10 秒

---

### ✅ 4. 创建 zed.tips 重建 Workflow

**文件**：`../zed.tips/.github/workflows/rebuild-from-tips-data.yml`

**触发条件**：
- 接收来自 zed-tips-data 的 `repository_dispatch` 事件
- 或手动触发（`workflow_dispatch`）

**工作流程**：
```
接收 repository_dispatch 事件
    ↓
GitHub Actions 触发 rebuild-from-tips-data.yml
    ├─ Checkout zed.tips
    ├─ 删除旧的 community-tips
    ├─ 克隆最新的 zed-tips-data
    ├─ Setup Node.js 和 pnpm
    ├─ pnpm install
    ├─ pnpm build
    └─ Commit & push 到 zed.tips main 分支
         ↓
Cloudflare Pages 自动检测 push 到 main
    ├─ 触发自动构建
    ├─ 构建输出到 dist/
    └─ 自动部署到 https://zed.tips
         ↓
✅ 新的 tips 已发布
```

**耗时**：约 2-3 分钟（包括 Cloudflare 部署）

---

## 🔧 需要手动配置的步骤

### 第 1 步：创建 GitHub App ⭐ **重要**

这是整个流程的核心，需要手动完成。

**参考**：`SETUP_GUIDE.md` 的第 1 步

操作步骤：
1. 进入 https://github.com/organizations/zed-tips/settings/apps
2. 创建新 App，名称：`zed-tips-ci`
3. 配置权限：
   - `Contents: Read and write`
   - `Actions: Read and write`
4. 生成私钥，记下 **App ID**

---

### 第 2 步：配置 Secrets ⭐ **重要**

**参考**：`SETUP_GUIDE.md` 的第 2 步

在 zed-tips-data 仓库中添加两个 Secrets：

| Secret 名称 | 值 |
|---|---|
| `GH_APP_ID` | 你的 App ID（如 `123456`） |
| `GH_APP_PRIVATE_KEY` | 从 `.pem` 文件复制的私钥内容 |

位置：https://github.com/zed-tips/zed-tips-data/settings/secrets/actions

---

### 第 3 步：配置分支保护规则 ⭐ **可选但推荐**

**参考**：`SETUP_GUIDE.md` 的第 3 步

在 zed-tips-data 仓库中：

1. 进入 https://github.com/zed-tips/zed-tips-data/settings/rules
2. 编辑或创建 `main` 分支规则
3. 启用 "Require status checks to pass before merging"
4. 选择 `validate / validate` workflow

**效果**：失败的 PR 无法 merge

---

## 📊 完整工作流程示意图

```
┌─────────────────────────────────┐
│   开发者提交 PR 到 main         │
│   (修改 tips/*.mdx)              │
└──────────────┬──────────────────┘
               ↓
┌─────────────────────────────────┐
│  GitHub Actions: validate-tips  │ ← 自动运行
│  ✅ 检查文件格式                 │
│  ✅ 验证 Front Matter             │
│  ✅ 检查必须字段                  │
└──────────────┬──────────────────┘
               ↓
        ❌ 失败?
        ↙       ↘
       是        否
      ↓         ↓
  无法     可以
  merge    merge
           ↓
    ┌──────────────────┐
    │ 开发者点击      │
    │ "Merge PR"       │
    └────────┬─────────┘
             ↓
    ┌──────────────────────────┐
    │ PR 合并到 main            │
    └────────┬─────────────────┘
             ↓
    ┌─────────────────────────────────┐
    │ GitHub Actions: trigger-rebuild │ ← 自动运行
    │ (zed-tips-data)                  │
    │ 发送 dispatch 事件到 zed.tips    │
    └────────┬──────────────────────────┘
             ↓
    ┌──────────────────────────────────┐
    │ GitHub Actions: rebuild-from     │ ← 自动运行
    │ -tips-data (zed.tips)            │
    │ ✅ 克隆 zed-tips-data             │
    │ ✅ pnpm build                     │
    │ ✅ Commit & push                  │
    └────────┬──────────────────────────┘
             ↓
    ┌──────────────────────────────────┐
    │ Cloudflare Pages                 │ ← 自动运行
    │ ✅ 检测到 push                    │
    │ ✅ 自动构建                       │
    │ ✅ 自动部署                       │
    └────────┬──────────────────────────┘
             ↓
    ┌──────────────────────────────────┐
    │ ✅ 完成                           │
    │ https://zed.tips 已更新          │
    └──────────────────────────────────┘
```

---

## 🧪 测试验证方案

### 测试 1：验证失败的情况

1. 创建 tip 文件缺少必须字段
2. 创建 PR
3. 预期：CI 显示红色 ❌，错误信息清晰

### 测试 2：验证成功的情况

1. 创建格式正确的 tip 文件
2. 创建 PR
3. 预期：CI 显示绿色 ✅

### 测试 3：完整的 CI/CD 流程

1. 创建并 merge 一个有效的 tip PR
2. 观察 zed-tips-data 的 `Trigger zed.tips Rebuild` workflow
3. 观察 zed.tips 的 `Rebuild from Tips Data` workflow
4. 检查 zed.tips 是否有新的 commit
5. 验证 Cloudflare 是否自动部署

---

## 📁 文件结构

```
zed-tips-data/
├── .github/
│   └── workflows/
│       ├── validate-tips.yml          ← PR 验证
│       └── trigger-rebuild.yml        ← 触发重建
├── scripts/
│   └── validate-tips.js               ← 验证脚本
├── tips/
│   └── *.mdx                          ← Tip 文件
├── package.json                       ← 依赖和脚本定义
├── .gitignore
├── SETUP_GUIDE.md                     ← 配置指南
└── IMPLEMENTATION_SUMMARY.md          ← 本文件

zed.tips/
└── .github/
    └── workflows/
        └── rebuild-from-tips-data.yml ← 重建脚本
```

---

## 🚀 快速开始

### 本地开发

当你想在本地测试新的 tip 时：

```bash
# 克隆 zed-tips-data 数据
git clone https://github.com/zed-tips/zed-tips-data.git zed.tips/src/content/community-tips

# 或者如果已经存在，更新它
cd zed.tips/src/content/community-tips
git pull origin main
cd ../../../
```

然后在 zed.tips 中开发和测试：

```bash
cd zed.tips
pnpm dev  # 启动开发服务器，会包含最新的 community tips
```

### 提交新的 Tip

```bash
# 创建新分支
git checkout -b feature/my-new-tip

# 创建 tip 文件（必须遵循 kebab-case）
cat > tips/my-awesome-tip.mdx << 'EOF'
---
title: "My Awesome Tip"
subtitle: "A description of the tip"
category: "navigation"
difficulty: "beginner"
tags: ["keyboard", "productivity"]
mediaType: "image"
mediaUrl: "https://example.com/image.png"
publishedAt: 2025-01-15
---

## How to Use

Your content here...
EOF

# 提交
git add tips/my-awesome-tip.mdx
git commit -m "feat: add my awesome tip"

# 推送
git push origin feature/my-new-tip
```

然后在 GitHub 上创建 PR，CI 会自动验证。

---

## 🔍 故障排除快速链接

更多详细的故障排除步骤，见 `SETUP_GUIDE.md` 的 "故障排除" 部分。

常见问题：
- ❓ "PR 验证显示 'No status checks found'" → 等待 5-10 分钟
- ❓ "验证一直 Pending" → 查看 GitHub Actions 日志
- ❓ "trigger-rebuild 没有触发" → 检查 Secrets 和 App 权限

---

## 📚 相关资源

- [zed-tips-data 仓库](https://github.com/zed-tips/zed-tips-data)
- [zed.tips 仓库](https://github.com/zed-tips/zed.tips)
- [SETUP_GUIDE.md](./SETUP_GUIDE.md) - 详细配置步骤
- [Astro 文档](https://docs.astro.build/)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

---

## 🎓 架构设计理念

这个方案的设计遵循以下原则：

1. **早期验证**：在 PR 阶段就发现错误，而不是等到 merge 后构建失败
2. **自动化**：最小化手动干预，通过 GitHub Actions 自动化所有流程
3. **独立性**：zed-tips-data 和 zed.tips 保持独立，但通过 GitHub App 安全通信
4. **透明性**：所有过程的日志都在 GitHub Actions 中可见，便于调试
5. **灵活性**：
   - category 和 difficulty 不限制为特定枚举值，允许未来扩展
   - tags 最多 10 个，避免过度标记
   - mediaUrl 只需要是有效 URL，不需要真的可访问

---

## 📝 后续改进建议

如果需要，可以考虑这些改进：

1. **添加 PR 评论**：在验证失败时自动添加评论，显示具体错误
2. **媒体 URL 检查**：验证 mediaUrl 实际可以访问（目前只检查格式）
3. **重复检查**：检查新的 tip 是否与现有的重复
4. **性能优化**：缓存依赖以加快 CI 运行时间
5. **自动修复**：对于可以自动修复的问题（如文件名格式），自动修正
6. **发布日志**：生成自动化的更新日志

---

## ✅ 检查清单

部署前请确认：

- [ ] GitHub App 已创建（`zed-tips-ci`）
- [ ] App 已在 zed-tips-data 和 zed.tips 中安装
- [ ] zed-tips-data 的两个 Secrets 已配置（`GH_APP_ID` 和 `GH_APP_PRIVATE_KEY`）
- [ ] 分支保护规则已配置（可选）
- [ ] 已经测试了验证失败的情况
- [ ] 已经测试了验证成功的情况
- [ ] 已经完整测试了 CI/CD 流程

---

**创建时间**：2025-01-15  
**最后更新**：2025-01-15

# 为什么需要创建 GitHub App？

这份文档解释了为什么我们选择 GitHub App 而不是其他方案，以及它的核心作用。

---

## 🎯 核心问题

**问题**：当 zed-tips-data 的 PR merge 时，我们需要触发另一个私有仓库（zed.tips）的自动构建和部署。

这看起来简单，但实际上涉及几个复杂的问题：

1. **跨仓库通信** - 两个仓库如何通信？
2. **权限管理** - 一个仓库如何有权操作另一个仓库？
3. **安全性** - 如何安全地传递凭证，不暴露在日志中？

---

## ❌ 为什么不能用其他方案？

### 方案 1：使用 Personal Access Token (PAT)

**原理**：
```yaml
# 这样做是不安全的！❌
env:
  GH_TOKEN: ${{ secrets.MY_PERSONAL_TOKEN }}
```

**为什么不行**：

| 问题 | 说明 | 后果 |
|------|------|------|
| **过度权限** | PAT 拥有用户的所有权限 | 即使只需要触发 workflow，也会获得修改代码、删除仓库等权限 |
| **难以审计** | Token 以个人身份操作 | 无法分辨谁在操作，只知道是某个人 |
| **不可撤销** | 无法单独撤销 Token | 如果泄露，必须重置整个人的 GitHub 密码 |
| **不可扩展** | 如果这个人离职怎么办？ | 必须重新生成 Token，更新所有配置 |
| **没有时间限制** | Token 长期有效 | 永远保有权限，直到手动删除 |

**实际例子**：
假设你用个人 Token，有人不小心看到日志或仓库被黑客入侵，攻击者就可以：
- ✅ 修改你的代码
- ✅ 删除你的仓库
- ✅ 访问你的 private 仓库
- ✅ 修改你的组织设置
- ...等等

---

### 方案 2：使用 Repository Secret 存储凭证

这不是真正的方案，只是如何存储凭证的方式。问题是：
- **用什么凭证来存储？** → 还是需要 Personal Token 或 GitHub App
- **谁来管理这个凭证？** → 问题依然存在

---

### 方案 3：在 zed.tips 上配置 Webhook

**原理**：zed-tips-data 通过 Webhook 向 zed.tips 发送 HTTP 请求。

**为什么不行**：

| 问题 | 说明 |
|------|------|
| **安全性差** | URL 和 Secret 可能暴露，需要考虑 DDoS 防护 |
| **配置复杂** | 需要公网服务器来接收 Webhook，维护成本高 |
| **不支持私有仓库** | 大多数 Webhook 需要公网访问，不适合私有仓库 |
| **GitHub 原生支持差** | 不是 GitHub 推荐的方案 |

---

### 方案 4：使用 `repository_dispatch` + `GITHUB_TOKEN`

**原理**：
```yaml
# 使用默认的 GITHUB_TOKEN
env:
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

**为什么不行**：

1. **默认 `GITHUB_TOKEN` 的限制**：
   - `GITHUB_TOKEN` 无法访问其他私有仓库
   - 它只能访问当前工作流所在的仓库
   - 跨仓库操作会被拒绝

2. **即使是公开仓库**：
   - 如果 zed.tips 是私有仓库，`GITHUB_TOKEN` 无法向其发送事件
   - GitHub 的安全策略禁止 Token 跨越仓库边界

**实际错误**：
```
❌ Error: Token does not have permission to trigger workflow in private repository
```

---

## ✅ GitHub App 为什么是正确选择？

### GitHub App 的核心优势

#### 1. **权限最小化** 🔒

```
Personal Token:
  ├─ 删除仓库权限 ❌
  ├─ 修改代码权限 ❌
  ├─ 管理成员权限 ❌
  ├─ 触发 Workflow 权限 ✅ （我们只需要这个）
  └─ ...50+ 其他权限 ❌

GitHub App:
  ├─ 删除仓库权限 ❌
  ├─ 修改代码权限 ✅ （需要更新社区 tips）
  ├─ 管理成员权限 ❌
  ├─ 触发 Workflow 权限 ✅
  └─ ...没有其他权限
```

#### 2. **易于管理和审计** 📋

- **清晰的身份**：操作显示为 "zed-tips-ci[bot]"，而不是某个人
- **易于追踪**：查看日志时清楚知道是自动化系统在操作
- **易于撤销**：只需卸载 App，立即失效
- **易于扩展**：新人加入，无需重新生成 Token

#### 3. **安全性高** 🛡️

- **私钥** 不是 Token，而是用来签署请求
- **自动过期**：可以设置有效期（虽然我们没有设置）
- **无法伪造**：使用 RSA 签名，难以伪造
- **权限隔离**：App 只能访问被安装的仓库

#### 4. **支持跨仓库操作** 🌉

只要 App 在两个仓库中都被安装，就可以：
- ✅ 在 zed-tips-data 中读取数据
- ✅ 向 zed.tips 发送 repository_dispatch 事件
- ✅ 在 zed.tips 中写入数据（commit & push）

#### 5. **支持私有仓库** 🔐

- Personal Token：需要特殊配置，容易出错
- GitHub App：原生支持，设计时就考虑了私有仓库

---

## 🔄 GitHub App 如何工作？

### 流程图

```
┌─────────────────────────────────┐
│  你在 GitHub 上创建 App         │
│  并生成私钥 (.pem 文件)          │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  在两个仓库中安装 App            │
│  (zed-tips-data 和 zed.tips)    │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  在 zed-tips-data 的 Secrets中  │
│  存储 App ID 和私钥             │
└──────────────┬──────────────────┘
               │
               ↓
    当 zed-tips-data PR merge 时：
               │
               ↓
┌─────────────────────────────────┐
│ GitHub Actions Workflow:         │
│ 1. 读取 Secrets 中的 App ID     │
│ 2. 读取 Secrets 中的私钥        │
│ 3. 使用私钥签署请求             │
│ 4. 向 GitHub API 发送签署的请求 │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│ GitHub API 验证签名:             │
│ 1. 检查签名是否有效              │
│ 2. 检查 App 是否被安装           │
│ 3. 检查 App 是否有权限           │
│ 4. 执行请求                      │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│ 触发 zed.tips 的 Workflow       │
│ (repository_dispatch 事件)       │
└─────────────────────────────────┘
```

### 为什么这样设计是安全的？

1. **私钥永不暴露**
   - 只在 zed-tips-data 的 Secrets 中存储
   - 用于签署请求，不是发送给 GitHub 的
   - GitHub 有公钥验证签名

2. **权限受限**
   - App 只能访问被安装的仓库
   - 只有被授予的权限可以使用
   - 无法访问其他仓库或执行不允许的操作

3. **可审计**
   - 所有操作都记录为 "zed-tips-ci[bot]"
   - 可以清晰地追踪谁做了什么
   - GitHub 提供完整的操作日志

---

## 📊 方案对比表

| 方案 | 权限最小化 | 支持私有仓库 | 易于管理 | 安全性 | 跨仓库 | 推荐度 |
|------|-----------|------------|--------|--------|--------|--------|
| **GitHub App** | ✅ 最好 | ✅ 原生支持 | ✅ 优秀 | ✅ 最好 | ✅ 完美 | ⭐⭐⭐⭐⭐ |
| Personal Token | ❌ 过度权限 | ⚠️ 需要配置 | ❌ 困难 | ❌ 不安全 | ⚠️ 可以 | ⭐ |
| Webhook | ⚠️ 需要密钥 | ❌ 不支持 | ⚠️ 复杂 | ⚠️ 中等 | ✅ 可以 | ⭐⭐ |
| 默认 GITHUB_TOKEN | ✅ 最小 | ❌ 不支持 | ✅ 简单 | ⚠️ 有限制 | ❌ 不行 | ⭐ |

---

## 🎓 GitHub 官方建议

### GitHub 的官方安全指南说：

> **对于需要与其他仓库交互的自动化任务，使用 GitHub App 是最佳实践。**
>
> - GitHub App 提供细粒度的权限控制
> - GitHub App 提供更好的安全性和可审计性
> - GitHub App 是为跨仓库场景设计的

来源：[GitHub Docs - Creating a GitHub App](https://docs.github.com/en/developers/apps/building-github-apps)

---

## 💡 我们的具体场景

### 需要实现的功能

```
zed-tips-data 仓库             zed.tips 仓库
    (公开)                      (私有)
      │                           │
      │ PR merge                   │
      ├─────────────────────────────>
      │  需要触发这个仓库的 workflow
      │
      └─ 但两个仓库完全独立
         无法直接通信
```

### GitHub App 的解决方案

```
zed-tips-data              GitHub              zed.tips
                          (中介)
      │                    │                     │
      │ merge              │                     │
      ├────────────────────>                     │
      │ 触发 workflow      │                     │
      │                    │ 验证签名             │
      │                    │ 检查权限             │
      │                    ├────────────────────>
      │                    │ 发送 dispatch 事件  │
      │                    │                 启动 workflow
      │                    │<──────────────────┤
      │                    │                     │
      │<────────────────────────────────────────┤
      │  自动更新完成                          │
      │                                       构建&部署
```

---

## 🔑 关键点总结

### GitHub App 为什么是必需的？

1. **跨私有仓库** 
   - zed.tips 是私有仓库，Personal Token 很难处理
   - GitHub App 原生支持私有仓库

2. **权限最小化**
   - 我们只需要 "触发 Workflow" 和 "写入代码" 权限
   - GitHub App 让我们精确指定权限
   - Personal Token 会授予所有权限

3. **安全性和可审计性**
   - 私钥签署请求，不暴露凭证
   - 操作清晰记录为机器人身份
   - 易于管理和撤销

4. **符合行业最佳实践**
   - GitHub 官方推荐
   - 大企业标准做法
   - 未来扩展时更容易

### 换个角度理解

如果你给某个人一张 **全能钥匙**（Personal Token），他可以：
- 打开你的门（访问仓库）
- 修改你的家具（修改代码）
- 卖掉你的房子（删除仓库）
- 邀请陌生人进来（修改权限）

但你只需要他 **打开门** 和 **放一个快递**。

GitHub App 就像是 **只能打开门和放快递的钥匙**，安全得多。

---

## 🚀 总结

**你需要创建 GitHub App，因为**：

1. ✅ **能跨越私有仓库边界** - Personal Token 做不到
2. ✅ **权限最小化** - 只给需要的权限
3. ✅ **安全可靠** - 私钥签名，无法伪造
4. ✅ **易于管理** - 清晰的身份和操作记录
5. ✅ **符合最佳实践** - GitHub 官方推荐

**没有 GitHub App，你的自动化系统就无法工作。**

---

## 📚 相关资源

- [GitHub Apps 文档](https://docs.github.com/en/developers/apps)
- [创建 GitHub App](https://docs.github.com/en/developers/apps/building-github-apps/creating-a-github-app)
- [使用 GitHub App 的安全性](https://docs.github.com/en/developers/apps/building-github-apps/security-best-practices-for-github-apps)
- [Repository Dispatch 事件](https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows#repository_dispatch)
